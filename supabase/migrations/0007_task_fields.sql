-- ============================================================================
-- Personal OS · Planner — редактируемые поля задачи — миграция 0007
-- ----------------------------------------------------------------------------
-- description нужен, чтобы заметка/идея/AI могли превратиться в задачу с телом.
-- tags — на будущее (пользовательские фильтры, P3). Идемпотентно.
-- ============================================================================

alter table tasks add column if not exists description text;
alter table tasks add column if not exists tags        text[] not null default '{}';
